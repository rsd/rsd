#!/usr/bin/env bash

if [[ ! -v RSD_ON || $RSD_ON -ne 1 ]]; then
    echo "This is a RSD library file. It should not be executed directly."
    echo "Please call 'rsd' to use it."
    exit 12
fi


[[ "$RSD_KPX_LIB" != "1" ]] && $(rsd::source_lib_or_die lib/kpx.lib)

RSD_C_KPX_SHORT_DESC="KeePass Integration"
RSD_C_KPX_VERSION="0.0.2"

function rsd::c::kpx::check() {
    rsd::l::kpx::check
    if ((! $?)); then
        echo "kpx command is ready for use."
        return 0
    fi
    return $?
}

# TODO:
# - [ ] check if vault exists
# - [ ]  check if password exists
# - [ ]  check if password is saved with gpg
# - [ ]  open the vault
# - [ ]  save a key in the vault
# - [ ]  get a key from the vault
